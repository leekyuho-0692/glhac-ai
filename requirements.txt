fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy>=2.0
pydantic>=2.5
httpx>=0.26
pytest>=8.0
alembic>=1.13          # DB 마이그레이션(§6.1) — SQLite/Postgres 양립
pymupdf>=1.24          # 서버 PDF 생성(§7, 내장 CJK 폰트)
# 비밀번호 해시는 stdlib PBKDF2 사용(추가 의존성 없음).
psycopg2-binary>=2.9   # Postgres 드라이버(§6.1)
openpyxl>=3.1          # xlsx 파싱(인테이크)
python-docx>=1.1       # docx 파싱(인테이크)
pycryptodome>=3.20     # AES/Hash/KDF — app/crypto.py 최상단 임포트(앱 기동 필수, models.py가 EncryptedType 사용)
pillow>=10.0           # 사진 EXIF 촬영일시·GPS 추출 — app/main.py 지연 임포트(없으면 조용히 None)

# OCR (문서 이미지 텍스트 추출 — app/ai_local.py, app/ocr_pipeline.py) · D1 배포 재현성
paddleocr==3.7.0
paddlepaddle==3.3.1
