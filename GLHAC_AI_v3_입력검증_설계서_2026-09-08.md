# GL-HAC AI v3 — 입력 검증 설계서

**작성 2026-09-08 · 대상 커밋 `6ac9d5f0b` · 검증 테스트 137건 (전체 666건 통과)**

이 문서는 신청부터 인증서 발급까지 각 단계에서 **무엇을 막고, 무엇은 알려만 주고, 무엇은 일부러 안 하는지**를 적는다. 내용은 기억이 아니라 코드에서 추출했다 — `app/schemas.py`(입력 형식), `app/state_machine.py`(단계 진입 조건), `app/main.py`(엔드포인트 검사).

---

## 1. 왜 만들었나

2026-09-07 이전 상태를 실측한 결과다.

| | 당시 |
|---|---|
| 단계 진입 가드 | 28개 상태 중 **9개**에만 존재 |
| 입력 형식 제약 | `schemas.py` 622줄에 **5개** (금액 `ge=0` 2곳, 사유 최소길이 2곳 등) |
| 업로드 | 크기·확장자·매직바이트 — 유일하게 잘 돼 있던 층 |
| 프런트 | 필수값 안내 ~140곳, **서버에 같은 검증이 없어 API 직접 호출이면 통과** |

구조가 "입력은 다 받고 마지막 전이에서 막는" 모양이었다. 그래서 잘못된 값이 DB에 들어간 뒤 한참 지나 다른 화면에서 터지거나, 아예 안 터지고 인증서에 인쇄됐다.

실제로 다음이 벌어지고 있었다.

- 원재료 유형·출처가 **자유 문자열**이라 오타가 그대로 저장됐다. 사전에 표준 코드가 있는데 대조하지 않았다.
- 날짜가 문자열로 저장돼, 나중에 `date.fromisoformat()`으로 읽는 만료 경보·인증서 유효성 판정이 죽는 경로가 있었다.
- **AI가 서류에서 읽은 발급일·만료일이 검증 없이 `status=active` 인증서 행으로 저장**됐다.
- `profile_ext` 숫자 변환 실패가 조용히 무시돼 값이 사라졌다. 자기선언 자격이 그 숫자로 갈리는데 "못 읽었다"는 말을 아무도 못 들었다.
- 비밀번호 정책이 전혀 없었다.

---

## 2. 세 가지 원칙

이 설계서의 판단 기준이다. 항목별 결정은 전부 여기서 나온다.

### 원칙 1 — 값이 들어오는 자리에서 막는다

나중에 읽는 쪽이 죽지 않게 하는 것이 목적이다. 날짜 검증의 실제 이유는 "형식이 예뻐서"가 아니라, `main.py`의 만료 경보와 인증서 유효성 판정이 `date.fromisoformat()`으로 그 값을 읽기 때문이다. 그래서 형식만 보지 않고 **파싱한 뒤 `YYYY-MM-DD`로 되돌려 저장**한다. 파이썬 3.11의 `fromisoformat`은 `20260910`이나 `2026-W37-1`도 받는데, 원문을 그대로 두면 화면·보고서·엑셀마다 다른 모양이 찍힌다.

### 원칙 2 — 상대가 손쓸 수 있는 것만 막는다

신규 가입 업체가 이미 자사 제품을 등록해 뒀을 리 없다. 제품 이름이 겹친다면 그건 **남의 제품**이고, 그 업체는 남의 사정을 알 수도 확인할 수도 없다. 막아 놓고 "이미 있습니다"라고만 하면 손쓸 방법이 없는 벽이 된다. 그래서 **등록시키고 표시만** 한다.

같은 이유로 원재료 인증번호는 형식을 강제하지 않는다(§5 참조).

### 원칙 3 — 통과시킨 것과 확인한 것은 다르다

선택 입력이라 막지 않더라도, **확인하지 못했다는 사실은 남긴다.** 자기선언은 업체가 자격을 스스로 주장하는 경로라, 근거가 비어 있다는 사실이 어디에도 없으면 나중에 "왜 이 업체가 자기선언으로 갔나"를 되짚을 수 없다.

---

## 3. 단계별 검증 — 무엇을 막는가

### 3.1 입력 형식 (`app/schemas.py`)

값이 서버에 닿는 즉시 본다. 위반은 **HTTP 422**.

| 모델 | 필드 | 규칙 |
|---|---|---|
| `RegisterReq` | `username` | 영문·숫자로 시작하는 3~32자, 영숫자와 `. _ -` 만 |
| | `password` | 8자 이상, 한 글자 반복 금지 |
| | `nib` | 아래 참조 |
| `AdminUserReq` | `username`·`password` | 위와 동일 (가입만 막고 관리자 생성이 뚫려 있으면 정책이 아니다) |
| `AdminUserPatchReq` | `password` | 변경할 때만 검사 (미변경은 허용) |
| `CaseProfileReq` | `company_name` | 공백만 넣은 것은 안 넣은 것 |
| | `nib` | 숫자 **10자리(한국 사업자등록번호)** 또는 **13자리(인도네시아 NIB)**. 붙임표 정규화 |
| | `email` | `@` 앞뒤 존재 + 도메인에 점 |
| | `phone` | 정규화 후 숫자 7~15자리 (E.164), 붙임표 없는 형태로 저장 |
| | `address`·`factory_address` | 최소 8자 (인증서에 인쇄됨) |
| | `due_date` | ISO 날짜 → `YYYY-MM-DD` 정규화 |
| | `profile_ext` | `annual_revenue`·`outlet_count`·`employee_count`·`total_employee` — 음수·비숫자 거부, 천단위 쉼표 허용 |
| `ProductCreate` | `name` | 공백 거부 |
| `MaterialCreate` | `name` | 공백 거부 |
| | `mat_type`·`source` | 사전 코드와 대조 (§4) |
| `MaterialPatch` | `mat_type`·`source` | 생성과 같은 규칙 (수정 경로만 열려 있으면 검증을 우회할 수 있다) |
| `PenyeliaCreate` | `name` | 공백 거부 |
| | `cert_expiry` | ISO 날짜 |
| `AuditPlanReq` | `scheduled_date` | ISO 날짜 + **과거 금지** |
| `AuditPlanPatchReq` | `scheduled_date` | ISO 날짜만 (수정은 완료 기록 보정에도 쓰이므로 과거 허용) |
| `FindingReq`·`CarSubmitReq` | `due_date` | ISO 날짜 |
| `RegulationUpsertReq` | `effective_date` | ISO 날짜 |

**사업자 식별번호(NIB)에 대하여** — 이 서비스는 **한국 업체가 인도네시아 할랄 인증을 받는** 흐름이다. 실데이터가 `126-81-67748` 같은 한국 사업자등록번호(10자리)였다. "인니 NIB 13자리"만 받으면 실사용 값이 전부 거부된다. 또한 기존 행 3건은 붙임표가 **en-dash(`–`)** 였다(PDF 복사). 눈으로는 같아 보여서 대조·중복검사가 조용히 어긋나므로 저장 전에 통일한다.

### 3.2 업로드 (`app/main.py` `_validate_upload`)

| 항목 | 값 |
|---|---|
| 최대 크기 | 10 MB (`413 UPLOAD_TOO_LARGE`) |
| 확장자 허용 | pdf, png, jpg, jpeg, webp, gif, xlsx, xls, docx, doc, csv, txt, hwp, mp4, mov, webm, m4v (`415 UNSUPPORTED_TYPE`) |
| 내용 검사 | 매직바이트로 실제 형식 확인 |
| 빈 파일 | `400 EMPTY_UPLOAD` |

### 3.3 배정 대상 실재·역할

배정한 대상이 실제로 있는 사람인지, 역할이 맞는지 본다. 오타 난 id로 배정하면 "배정은 됐는데 아무도 못 보는" 상태가 된다.

| 엔드포인트 | 검사 |
|---|---|
| `POST /ops/cases/{id}/assign-auditor` | `auditor` 역할 + 같은 org (`404 AUDITOR_NOT_FOUND`) |
| `POST /cases/{id}/assign-consultant` | `consultant` 역할 (`404 CONSULTANT_NOT_FOUND`) |
| `POST /cases/{id}/pendamping/assign` | `pendamping_pph` 역할 (`404 PENDAMPING_NOT_FOUND`) |

LPH 배정의 `auditor_ref`는 외부 기관 값이라 자유 문자열로 둔다.

### 3.4 단계 진입 가드 (`app/state_machine.py`)

28개 상태 중 9개에 진입 조건이 있다. 위반은 **HTTP 409** + `blockers` 목록.

| 진입 상태 | 가드 | 차단 코드 |
|---|---|---|
| `ai_pre_assessment_ready` | `guard_intake_complete` | `COMPANY_NAME_MISSING` · `NIB_MISSING` · `NO_PRODUCT` |
| `document_pre_audit_in_review` | `guard_payment` | 위 3종 + `UNPAID_INVOICE` |
| `document_pre_audit_approved` | `guard_documents_approved` | `DOCUMENTS_NOT_APPROVED` |
| `audit_closed` | `guard_car_closed` | `OPEN_FINDINGS` · `UNRESOLVED_CAR` |
| `final_package_preparation` | `guard_final_package` | `UNRESOLVED_MAJOR_NC` |
| `self_declare_eligible` | `guard_pathway_selfdeclare` | `RISK_NOT_LOW` · `NOT_MSME` · `HAS_CRITICAL_MATERIAL` · `MATERIAL_EVIDENCE_INCOMPLETE` · `REVENUE_EXCEEDS_LIMIT` · `TOO_MANY_FACILITIES` · `TOO_MANY_OUTLETS` |
| `self_declaration_submitted` | `guard_selfdeclare_submit` | `PENYELIA_HALAL_MISSING` · `PENDAMPING_NOT_VERIFIED` · `MATERIAL_EVIDENCE_INCOMPLETE` · `SIHALAL_IDENTITY_UNVERIFIED` |
| `lph_assignment` | `guard_reguler_submit` | `SIHALAL_IDENTITY_UNVERIFIED` |
| `certificate_issued` | `guard_certificate_issue` | `FATWA_NOT_APPROVED` · `SCOPE_NOT_FROZEN` |

전 구간 상시 차단(`evaluate_blocking`): `PENYELIA_HALAL_MISSING` · `PENYELIA_CERT_EXPIRED` · `SIHALAL_IDENTITY_UNVERIFIED`

**할랄감독자** — 종전에는 `status=active`이기만 하면 통과라 자격이 만료된 감독자로도 진행됐다. 이제 `없음(지정하세요)`과 `자격 만료(재교육 받으세요)`를 갈라 말한다. 할 일이 다르기 때문이다. **만료일 미입력은 만료로 치지 않는다** — 아직 안 받은 기존 데이터로 멀쩡한 케이스를 멈추면 안 된다.

**자기선언 임계값** (BPJPH): 연매출 ≤ Rp15,000,000,000 · 공장 ≤ 1 · 매장 ≤ 1

---

## 4. 원재료 코드 — 사전이 정본

코드 목록을 `main`·`intake`·`schemas` 세 곳에 두면 한쪽만 늘어난다. `app/domain_dict.py`가 정본이고 나머지가 가져다 쓴다.

```
MATERIAL_TYPE_CODES   raw · additive · processing_aid · preservative · cleaning · lubricant · packaging
MATERIAL_SOURCE_CODES animal · plant · microbial · synthetic · mineral · unknown
```

`material_type_code()`가 표기 흔들림을 흡수한다 — `Raw Material` → `raw`, `sanitizer`/`sanitiser` → `cleaning`, `원재료` → `raw`. **뜻이 같은 말은 받아 주되 저장은 표준 코드로 한다.** 못 알아보는 값은 거부한다(지어내지 않는다).

`main._mat_type_code`는 이 함수에 위임한다.

---

## 5. 막지 않는 것 — 그리고 그 이유

### 5.1 제품 이름 중복 → 표시만

`POST /cases/{id}/products`는 같은 이름이 있어도 **200으로 등록**하고 `duplicate_of`·`warning`을 함께 낸다. 사전심사 제품 목록에 `⚠ 이름 중복 N` 배지가 뜬다.

원칙 2에 따른 결정이다. 덧붙여 같은 이름의 다른 규격·용량을 따로 올리는 일이 실제로 있고, 사정은 등록한 쪽이 안다.

### 5.2 연매출·매장 수 → 선택 입력

미입력은 통과시킨다. **입력했는데 상한을 넘으면** 그때는 판정 가능한 사실이므로 막는다.

매장이 없는 제조업체가 대부분인데 "없는 것을 0으로 적으라"고 요구하면 그게 더 이상하다.

다만 원칙 3에 따라 `assess_pathway`가 확인하지 못한 항목을 함께 낸다(차단 아님).

```json
"unverified": [
  {"field": "annual_revenue", "label": "연매출", "limit": 15000000000},
  {"field": "outlet_count",   "label": "매장 수", "limit": 1}
]
```

화면(경로판정 카드)에는 이렇게 뜬다.

> ⚠ **미확인 자격 요건** · 연매출, 매장 수
> 입력하지 않아 확인하지 못했습니다 — 자기선언 자격 판단의 근거가 비어 있습니다.

### 5.3 원재료 인증번호 → 형식 검증 안 함

실데이터가 발급기관마다 전혀 다르다.

```
398240000 · ID00410000500391022 · LPPOM-00230049860209
ARA-504254310625 · KAI.5986.12633.250019.CN · DSM.MAN.2504.5036.COL
```

공통 형식이 없는데 규칙을 만들면 **멀쩡한 남의 인증번호를 거부**하게 된다. 나중에 누가 형식 검증을 넣지 않도록 이유를 테스트로 붙들어 뒀다(`test_material_cert_no_is_deliberately_not_format_checked`).

### 5.4 원재료 유형·출처 → 선택 입력

비워 두면 통과한다. 값을 넣었을 때만 사전과 대조한다.

### 5.5 서류 유형 오판 → 이미 사람이 보는 자리가 있다

문서 카드의 `AI 판단 · 신뢰도 N%` + 승인/반려/재분류가 그 자리다. 새로 막을 것이 없다. 제품 사진·설명 미비도 `⚠ 보고서 미비` 배지로 보인다.

---

## 6. AI가 만든 값의 관문

AI 추출값은 사람이 적은 값과 다르게 다룬다.

### 6.1 인증서 날짜 (`_process_doc`)

`halal_certificate` 문서에서 읽은 `issue_date`·`expiry_date`가 검증 없이 `status=active` 인증서 행으로 저장되고 있었다. 엉터리 값이 들어가면 나중에 `date.fromisoformat`으로 읽는 곳에서 죽는다.

`_safe_date()` 관문을 둔다.

- 못 읽는 날짜 → `None`
- 만료 ≤ 발급 → **둘 다 버리고** `halal_cert_note: DATE_RANGE_INVALID`

지어낸 유효기간으로 "유효한 인증서"를 만들지 않는다. 시스템이 직접 발급하는 인증서(`_do_issue_certificate`)는 계산값이라 원래 안전했다 — 위험은 추출 경로에만 있었다.

### 6.2 문서 분석 실패

호출 실패와 "모델이 보고도 못 찾음"을 구분한다. 둘 다 `other`/신뢰도 0으로 떨어뜨리면 화면에는 똑같이 "분석 완료"로 보인다.

- 실패는 `fields._llm_error`로 남고 **확인 대기 목록**에 재처리 대상으로 나온다
- 문서 카드에 `⚠ AI 분석 실패 · 재처리 필요` 배지, 목록 맨 앞으로 정렬
- **`AI 없음` 배포는 실패로 세지 않는다** — 설계된 상태다

### 6.3 확인 대기 (AI 추출 목록)

LLM만 뽑은 제품·원재료는 자동 반영하지 않고 사람이 확인한다. **이미 케이스에 있는 값은 목록에서 뺀다** — 안 그러면 반영한 뒤에도 같은 항목이 영원히 남아 목록이 소음이 된다.

### 6.4 채팅·번역

- 채팅은 `unavailable` / `failed` / `empty`를 다른 말로 한다. `failed`만 재시도 가능
- 답을 못 준 말풍선에는 **근거(citation)를 달지 않는다** — 답은 없는데 출처만 있는 꼴이 된다
- 번역은 조각 하나라도 호출이 실패하면 **전체를 포기한다**(502). 빈 조각을 이어붙이면 다른 조각이 성공한 탓에 "번역됨"으로 보이고 그대로 캐시돼 문서 한가운데가 조용히 사라진다

---

## 7. 검증이 사람에게 닿는 방법

**검증했는데 사유가 안 보이면 검증이 아니다.**

FastAPI 기본 422는 `detail`이 **배열**이라, `{code}`만 보는 화면 코드에서 사유가 빈칸으로 뜬다. 사용자는 잘못 적은 건 아는데 무엇이 잘못됐는지 못 본다.

`RequestValidationError` 핸들러가 앱 규약으로 맞춘다.

```json
{"detail": {
  "code": "VALIDATION_ERROR",
  "field": "due_date",
  "message": "처리 기한은(는) YYYY-MM-DD 형식이어야 합니다(받은 값: 내년)",
  "errors": [...]
}}
```

프런트는 `EMSG(e)`를 쓴다 — `message` 우선, 없으면 `code`. **새 오류 표시를 넣을 때 `e.code`를 직접 쓰지 말 것.** 소스 수준 회귀 테스트가 이 규약을 강제한다(`test_no_screen_shows_a_bare_error_code`). 비교 용도(`e.code === 'X'`)는 정상이라 제외한다.

이 규약을 손으로 지키려다 세 번 놓쳤다. 관용구가 10종으로 갈려 있었고 catch 변수명이 `e`가 아닌 곳도 있었다.

### 브라우저 실측

| 화면 | 입력 | 표시 |
|---|---|---|
| 신청서 | `없는이메일` | 저장 실패 · email: 이메일 형식이 올바르지 않습니다(받은 값: 없는이메일) |
| 신청서 | `123` | 저장 실패 · phone: 전화번호 자릿수가 올바르지 않습니다(숫자 3자리) |
| 신청서 | `999` | 저장 실패 · nib: 사업자 식별번호 자릿수가 맞지 않습니다 |
| 신청서 | `-1` | 저장 실패 · 매장 수은(는) 음수일 수 없습니다 |
| 신청서 | `(공백)` | 저장 실패 · company_name: 기업명은(는) 비워 둘 수 없습니다 |
| 신청서 | `서울` | 저장 실패 · address: 회사 주소가 너무 짧습니다 — 인증서에 인쇄되므로… |
| 회원가입 | `pw` | 가입 실패 · password: 비밀번호는 8자 이상이어야 합니다 |
| 회원가입 | `my id` | 가입 실패 · username: 아이디는 영문·숫자로 시작하는 3~32자여야 합니다 |
| 제품 | 중복 이름 | 등록됨 + ⚠ 이름 중복 2 배지 |

연매출·처리 기한은 `input type=number`/`type=date`라 브라우저가 입력 단계에서 막는다(이중 방어). 원재료 유형·출처는 화면이 `select`라 잘못된 코드를 보낼 수 없다 — 서버 검증은 API 직접 호출·문서 자동추출 경로를 막는 역할이다.

---

## 8. 기존 데이터 영향

새 규칙을 걸 때마다 **기존 데이터가 전부 통과하는지 먼저 확인**했다.

| 항목 | 결과 |
|---|---|
| 계정 아이디 14개 | 위반 0 |
| 회사 주소 5건 | 위반 0 |
| 이메일·전화 | 위반 0 |
| 인증서 날짜 2건 | 모두 정상 |
| 제품명 중복 | 0 |
| 할랄감독자 자격 만료 | 0 |
| **사업자번호** | **1건 위반** — CV. CITRA PRATAMA의 값이 `123` |

이 1건은 **데모 데이터의 자리표시자**다. 실제 업체 정보가 아니다. 근거:

- 케이스 9건이 전부 `org_demo` 한 조직에 있다
- 서로 다른 세 회사(바이오로제트·질경이·천우건설)가 같은 번호 `207-81–34847`을 쓴다 — 한 곳의 번호를 복사해 만든 흔적
- CV. CITRA는 자카르타 주소·인니 담당자(RACHAMAWATI)인데 연락처가 `ryeong@cheonu.kr` / `+82…` — 천우건설 것이 붙어 있다

**CV(Commanditaire Vennootschap)는 인도네시아 법인 형태다.** 한국 사업자등록번호가 있을 리 없고, 인니 NIB도 발급 전이면 없다. 즉 **비워 두는 것이 정상적인 상태**다.

그런데 그 값을 지울 수가 없었다 — 화면에서 칸을 비워도 서버가 "안 보낸 것"으로 보고 건너뛰었고, 저장할 때마다 화면이 `123`을 다시 보내 422로 막혔다. 손쓸 수 없는 상태다. 그래서 **비운 칸은 '지운 것'으로 보도록** 고쳤다(`model_fields_set`으로 '안 보냄'과 '비워서 보냄'을 구분). 보낸 적 없는 필드는 그대로 둔다.

자기선언 경로는 진입 시점에만 판정하므로, 이미 그 단계를 지난 케이스(바이오로제트 발급완료·CV. CITRA 컨설턴트검토)는 영향받지 않는다.

---

## 9. 이 문서를 고칠 때

- 검증을 추가·변경하면 **이 문서와 테스트를 같이 고친다.** 테스트는 `tests/test_input_validation_p0.py`(67) · `p1.py`(23) · `p2.py`(47)
- 규칙을 넓히기 전에 **실데이터를 먼저 조회한다.** NIB을 "인니 13자리"로 가정했다가 실데이터(한국 10자리)와 어긋난 적이 있다. 원재료 인증번호를 검증하지 않기로 한 것도 같은 방식으로 확인했다
- 비밀번호·아이디 규칙을 바꾸면 **테스트가 만드는 계정의 생성/로그인 짝을 같이 고친다.** 한쪽만 고치면 `BAD_CREDENTIALS`로 실패한다
- 새 차단 코드를 넣으면 화면 문구(`BLOCKER_KO`)와 3언어 사전을 같이 채운다

### 관련 커밋

```
c3c7ac8a5  P0 — 원재료 코드·날짜·공백·비밀번호 + 422 응답 규약
99550dbcc  P1 — 사업자번호·자기선언·감독자 자격·배정 대상·심사 일정
8ec643547  P2 — 인증서 날짜 관문·제품 중복·숫자 범위·연락처
e41ccf6fa  검증 사유가 모든 화면에 닿게 (표시 지점 96곳)
ba21ff4e9  매장 수 선택 · 제품 중복은 표시만
e280349ab  연매출 선택 — 막지 않되 미확인은 기록
6ac9d5f0b  P2 잔여 — 아이디·주소 · 안 하기로 한 것의 이유
```
